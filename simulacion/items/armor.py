class Armor:
    def __init__(self, item_id, name, armor):
        self.item_id = item_id
        self.name = name
        self.armor = armor

def get_item(self, item_id):
        return self.items.get(item_id)

shield = Armor(1, "Escudo", 2)
shieldOfSun = Armor(2, "Escudo del Sol", 3)
armor = Armor(3, "Armadura", 5)
